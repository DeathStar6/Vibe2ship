# The port the Express application will listen on
PORT=8080

# Google Gemini API key used server-side for function orchestration
# NEVER commit the actual key to git! Specify this safely in your secrets manager
GEMINI_API_KEY=your_gemini_api_key_here
